# Minimum fixes reviewed in P1-G1 (2026-09-11).
# Constraints do not install packages; requirements-dev.lock pins the tested set.
click>=8.3.3
curl-cffi>=0.15.0
idna>=3.15
mako>=1.3.12
# The 6.x branch has a separate fix floor; keep the tested 5.x branch.
protobuf>=5.29.6,<6
pyasn1>=0.6.4
soupsieve>=2.8.4
starlette>=1.3.1
urllib3>=2.7.0
werkzeug>=3.1.6
