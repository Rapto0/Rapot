-r requirements.txt

# Shared local/CI test and lint tools.
pytest==9.0.3
pytest-asyncio==1.3.0
pytest-cov==7.0.0
httpx==0.28.1
# Starlette's supported TestClient transport; application SDKs still need httpx.
httpx2==2.12.0
ruff==0.14.13
pre-commit==4.5.1
